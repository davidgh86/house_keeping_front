export default function () {
  return {
    updatingApartments: {}, // updatingApartments
                                    // interval
                                    // 
    // token: localStorage.getItem('token') || '',
    // user: localStorage.getItem('username') || '',
    // role: localStorage.getItem('role') || ''
  }
}
